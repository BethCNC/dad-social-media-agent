# Frontend Production Environment Variables
# This file is used during the Docker build process
# The actual value will be injected by Coolify at build time

# IMPORTANT: In Coolify, set this environment variable:
# VITE_API_BASE_URL=https://your-domain.com

# Default fallback (will be replaced during build)
VITE_API_BASE_URL=__REPLACE_WITH_YOUR_DOMAIN__
